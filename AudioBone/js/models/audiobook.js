define([
    'Underscore',
    'Backbone'], 
    function(_, Backbone){
        var AudiobookModel = Backbone.Model.extend({
            initialize: function () { }        
        });
        return AudiobookModel;
    });